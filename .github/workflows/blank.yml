mkdir vijayavarahi_website
cd vijayavarahi_website

python -m venv venv

venv\Scripts\activate

pip install django whitenoise gunicorn

django-admin startproject config .
django-admin startapp core

vijayavarahi_website/
│
├── core/
│   ├── templates/
│   │   ├── core/
│   │   │   ├── home.html
│   │   │   ├── about.html
│   │   │   ├── services.html
│   │   │   ├── contact.html
│   ├── views.py
│   ├── urls.py
│
├── config/
│   ├── settings.py
│   ├── urls.py
│
├── static/
│   ├── css/style.css
│
├── manage.py
└── requirements.txt

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'core',
]

STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'
STATICFILES_DIRS = [BASE_DIR / 'static']

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',
    ...
]

ALLOWED_HOSTS = ['*']  # later update for domain

from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('core.urls')),
]

from django.shortcuts import render

def home(request):
    return render(request, 'core/home.html')

def about(request):
    return render(request, 'core/about.html')

def services(request):
    return render(request, 'core/services.html')

def contact(request):
    return render(request, 'core/contact.html')

    python manage.py migrate
python manage.py runserver

http://127.0.0.1:8000/

pip freeze > requirements.txt

web: gunicorn config.wsgi

git init
git add .
git commit -m "Initial Vijayavarahi website"
git branch -M main
git remote add origin https://github.com/yourusername/vijayavarahi.git
git push -u origin main
<!DOCTYPE html>
<html>
<head>
  <title>Vijayavarahi Consultancy</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">Vijayavarahi</a>
    <div>
      <a class="btn btn-warning" href="/contact/">Request Consultation</a>
    </div>
  </div>
</nav>

<section class="container text-center mt-5">
  <h1>Strategic Consulting That Drives Business Impact</h1>
  <p class="lead">
    We help banks and enterprises optimize operations, manage risk, and accelerate growth.
  </p>
</section>

</body>
</html>

